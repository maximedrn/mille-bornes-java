package com.mille_bornes.game.utils;

/**
 * Enum representing the various player states in the Mille Bornes game.
 */
public enum StateEnum {
    /** Represents the 'MARCHE' state. */
    MARCHE,
    
    /** Represents the 'PANNE' state. */
    PANNE,
    
    /** Represents the 'CREVAISON' state. */
    CREVAISON,
    
    /** Represents the 'ACCIDENT' state. */
    ACCIDENT,
    
    /** Represents the 'ARRET' state. */
    ARRET,
    
    /** Represents the 'LIMITATION' state. */
    LIMITATION,
    
    /** Represents the 'CITERNE' state. */
    CITERNE,
    
    /** Represents the 'INCREVABLE' state. */
    INCREVABLE,
    
    /** Represents the 'AS_DU_VOLANT' state. */
    AS_DU_VOLANT,
    
    /** Represents the 'PRIORITAIRE' state. */
    PRIORITAIRE
}